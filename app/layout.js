
import "./globals.css";



export const metadata = {
  title: "Yoda bot",
  description: "Yoda AI chat bot",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
      
      >
        {children}
      </body>
    </html>
  );
}
